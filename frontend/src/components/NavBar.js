import { Link } from "react-router-dom"
const NavBar = () => (
    <header>
        <div className="container">
            <Link>
                <h1>Workout Tracker</h1>
            </Link>
        </div>
    </header>
)

export default NavBar